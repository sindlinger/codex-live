{
  "name": "codex-live",
  "version": "1.0.19",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prebuild": "node ./scripts/bump-version.mjs",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  },
  "buildMeta": {
    "builtAtUtc": "2026-03-01T07:06:04.813Z"
  }
}
