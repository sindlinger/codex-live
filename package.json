{
  "name": "codex-live",
  "version": "1.0.28",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prebuild": "node ./scripts/bump-version.mjs",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  },
  "buildMeta": {
    "builtAtUtc": "2026-03-14T05:31:11.824Z"
  }
}
